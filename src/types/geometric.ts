export interface Point {
  x: number;
  y: number;
}

export interface Bound {
  xmin: number;
  xmax: number;
  ymin: number;
  ymax: number;
}

export interface MinMax {
  min: number;
  max: number;
}
